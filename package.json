{

  "scripts": {
    "cypress": "cypress open",
    "loginSection:tests": "cypress run --e2e --headless --browser chrome --spec cypress/e2e/1-login/login-section.cy.js",
    "homeSection:tests": "cypress run --e2e --headless --browser chrome --spec cypress/e2e/2-home-section/**.cy.js"
  },

  "devDependencies": {
    "cypress": "^14.0.2"
  }

  
}
